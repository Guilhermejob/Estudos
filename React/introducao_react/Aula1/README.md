# Criando projeto em react

para criar um projeto em react usamos o comando npm create vite@latest . e em seguida selecionamos as opções referentes ao react ou npm create vite@latest . --template react
para ja criar usando o template do react